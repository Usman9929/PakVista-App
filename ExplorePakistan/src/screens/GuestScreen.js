import { View, Text, TouchableOpacity } from 'react-native';

const GuestScreen = () => (
    <TouchableOpacity>
      <Text style={{paddingTop:5, color:"red"}}>Guest</Text>
      {/* To do Later  */}
    </TouchableOpacity>
  );

  
  
export default GuestScreen