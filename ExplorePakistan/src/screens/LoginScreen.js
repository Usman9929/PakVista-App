import { View, Text, TouchableOpacity } from 'react-native';

const LoginScreen = () => (
  <TouchableOpacity>
    <Text style={{paddingTop:15, paddingBottom:5, color:'red'}}>Login</Text>
    {/* To do Later*/}
    </TouchableOpacity>
);

export default LoginScreen